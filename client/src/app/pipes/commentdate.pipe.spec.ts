import { CommentdatePipe } from './commentdate.pipe';

describe('CommentdatePipe', () => {
  it('create an instance', () => {
    const pipe = new CommentdatePipe();
    expect(pipe).toBeTruthy();
  });
});
